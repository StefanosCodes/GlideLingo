name: Deploy development API

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - backend/**
      - .github/workflows/deploy-development-api.yml

permissions:
  contents: read
  id-token: write

concurrency:
  group: deploy-development-api
  cancel-in-progress: false

env:
  GCP_PROJECT_ID: glidelingo-development
  GCP_REGION: ${{ vars.GCP_REGION || 'us-west1' }}
  GCP_REPOSITORY: glidelingo-containers
  GCP_SERVICE: glidelingo-api
  IMAGE_NAME: glidelingo-api

jobs:
  deploy:
    name: Verify, build, deploy, and smoke
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment: development

    steps:
      - uses: actions/checkout@v7

      - name: Install uv and Python
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
        with:
          enable-cache: true
          python-version: "3.13"
          working-directory: backend

      - name: Verify backend
        run: npm run setup:backend && npm run api:verify

      - id: auth
        name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_DEPLOY_SERVICE_ACCOUNT }}

      - name: Install Google Cloud CLI
        uses: google-github-actions/setup-gcloud@v3

      - name: Configure Artifact Registry authentication
        run: gcloud auth configure-docker "${GCP_REGION}-docker.pkg.dev" --quiet

      - id: image
        name: Build and push immutable image
        shell: bash
        run: |
          set -euo pipefail
          image_tag="${GCP_REGION}-docker.pkg.dev/${GCP_PROJECT_ID}/${GCP_REPOSITORY}/${IMAGE_NAME}:${GITHUB_SHA}"
          docker build --tag "${image_tag}" backend
          docker push "${image_tag}"
          digest="$(gcloud artifacts docker images describe "${image_tag}" --format='value(image_summary.digest)')"
          echo "uri=${GCP_REGION}-docker.pkg.dev/${GCP_PROJECT_ID}/${GCP_REPOSITORY}/${IMAGE_NAME}@${digest}" >> "${GITHUB_OUTPUT}"

      - id: deploy
        name: Deploy Cloud Run revision
        run: |
          gcloud run deploy "${GCP_SERVICE}" \
            --project="${GCP_PROJECT_ID}" \
            --region="${GCP_REGION}" \
            --image="${{ steps.image.outputs.uri }}" \
            --quiet
          echo "url=$(gcloud run services describe "${GCP_SERVICE}" --project="${GCP_PROJECT_ID}" --region="${GCP_REGION}" --format='value(status.url)')" >> "${GITHUB_OUTPUT}"

      - name: Smoke liveness and database readiness
        shell: bash
        run: |
          set -euo pipefail
          api_url="${{ steps.deploy.outputs.url }}"
          curl --fail --retry 6 --retry-all-errors --retry-delay 5 "${api_url}/health/live"
          curl --fail --retry 6 --retry-all-errors --retry-delay 5 "${api_url}/health/ready"
