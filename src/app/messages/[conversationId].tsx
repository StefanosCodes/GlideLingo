import { MarketplaceMessageThreadScreen } from '@/features/tutor-marketplace/marketplace-message-thread-screen';

export default function MessageThreadRoute() {
  return <MarketplaceMessageThreadScreen />;
}
